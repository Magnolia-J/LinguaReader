# ── 依赖 ──
node_modules/

# ── 环境变量 / 密钥（含 API 密钥，绝不能提交）──
.env
.env.local
.env.*.local
.env.production
.env.development
*.env
*.local.env

# ── 密钥 / 证书文件 ──
*.key
*.pem
*.p12
*.pfx
*.keystore
*.jks
*.crt
*.cer
*.der
secrets.json
credentials.json

# ── 任何文件名含 Secret / Token / Credential 的配置文件 ──
*secret*
*token*
*credential*
*.secret
*.token

# ── 个人数据（书籍 / 知识库 / 阅读记录）──
data/
*.store.json
store.json

# ── 运行日志 ──
server.log
*.log
npm-debug.log*

# ── 构建产物（由 build_dist.js 生成）──
dist/

# ── 本地记忆 / Agent 配置 ──
.workbuddy/

# ── 系统文件 ──
.DS_Store
Thumbs.db

# ── 临时文件 / 临时部署目录 ──
.mchk.js
*.tmp.js
.mobile_deploy/
deploy/
test_kbfilter.js
test_renderkb.js
mobile/snapshot.json

# ── 本地测试产物（验证自愈用，不提交）──
.test_heal/
test_heal.js
sim_test/

# ── 本地自动备份文件夹（含个人隐私数据快照，不提交）──
WorkbenchBackup/
backup-config.json

# ── 个人/草稿数据（含真实书库 store.json 与书籍，绝不提交）──
test_data/
test_data/**

# ── 个人调试 / 草稿脚本（硬编码本机路径，不提交）──
_check.js
_clear_timers.js
reading-ritual-preview.html

# ── Electron 构建产物 ──
dist/
*.exe
*.app
*.dmg
release/
